# Install with the tested compatibility constraints below.
-c constraints.txt

# Core AI and retrieval dependencies
sentence-transformers==5.7.0
transformers==4.57.6
torch==2.2.2
scikit-learn==1.3.2

# API, Schema Validation, and Environment Layers
fastapi>=0.141.1
uvicorn>=0.52.4
pydantic>=2.13.5
python-dotenv>=1.0.0
# Starlette 1.6 uses httpx2 for TestClient; AnyIO 4.15 deprecates an alias
# still referenced by that Starlette release.
httpx2>=2.13.0
anyio>=4.14.2

# Persistent Logging, SQLite Mapping, and Metrics Observability
sqlalchemy>=2.0.0
prometheus-client>=0.17.0

# Engineering Testing Harness Frameworks
pytest>=9.1.1
requests>=2.31.0
